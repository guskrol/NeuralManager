NeuraLNickCapture.jar lives here so the start .bat files can copy it to:

%USERPROFILE%\DreamBot\Scripts\NeuraLNickCapture.jar

If the helper source changes, rebuild the jar for Java 11 and replace this file's sibling jar.
